import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection:'row',
    alignItems:'center',
    borderRadius:5,
    marginBottom:10,
    width:'100%',
  },
  nameParticipant:{
    fontSize:16,
    color: '#FDFCFE',
    fontWeight:'bold',
    marginLeft:16,
    flex:1,
  }

});